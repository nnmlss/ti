// Authentication and activation configuration
export const ACTIVATION_TOKEN_EXPIRY_MINUTES = 7;

// Convert minutes to milliseconds for Date calculations
export const ACTIVATION_TOKEN_EXPIRY_MS = ACTIVATION_TOKEN_EXPIRY_MINUTES * 60 * 1000;